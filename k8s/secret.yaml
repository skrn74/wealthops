# Template only - contains no real secrets.
# Copy this file to k8s/secret.local.yaml, fill in the real values there,
# then run: kubectl apply -f k8s/secret.local.yaml
# k8s/secret.local.yaml is gitignored and must NEVER be committed.

apiVersion: v1
kind: Secret

metadata:
  name: wealthops-secrets
  namespace: wealthops

type: Opaque
stringData:
  DB_USER: postgres
  DB_PASSWORD: postgres
  UPSTOX_CLIENT_ID: 0d732841-1a7b-4bb2-9f10-1a4ecd5765b3
  UPSTOX_CLIENT_SECRET: x2s7rncfkc
