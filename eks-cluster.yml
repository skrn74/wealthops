apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: wealthops-eks
  region: us-east-1

managedNodeGroups:
  - name: wealthops-ng
    instanceType: t3.medium
    minSize: 1
    desiredCapacity: 2
    maxSize: 3
    volumeSize: 30
    privateNetworking: false